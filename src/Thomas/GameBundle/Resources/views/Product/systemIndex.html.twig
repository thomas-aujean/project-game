{% extends "ThomasGameBundle::layout.html.twig" %}
{% block title %}
  La base - {{ parent() }}
{% endblock %}

{% block myfilter %}
{{ include("ThomasGameBundle:Product:form.html.twig") }}
{% endblock %}

{# {% block search %} #}
{# {{ render(controller("ThomasGameBundle:Product:search")) }} #}
{# {{ include("ThomasGameBundle:Product:search.html.twig") }} #}
{# {% endblock %} #}
{% block thomasgame_body %}
<h1>page des consoles</h1>

{# {% if test is not null %}
{% for t in test %}
iciiii
        {{ t.id }}
  {% endfor %}
{% endif %} #}

  {% for product in listSystems %}


    <div class="col-md-3">
    <a href="{{ path('thomas_game_view', {'id': product.id}) }}">
          {% if product.productImage is not null %}
          <img src="{{ asset(product.productImage.webPath) }}" alt="{{ product.productImage.alt }}" style="width:100%;max-height:200px"/>
          {% endif %}
       
      {{ product.name }} - {{ product.price }} €
    </a>
      {% if product.productCategory is not null %}
        <div>Catégorie - {{ product.productCategory.name }}</div>
      {% endif %}

      {% if product.system is not null %}
        <div>Console - {{ product.system.name }}</div>
      {% endif %}

      <a href="{{ path('thomas_game_add', {'id': product.id}) }}">Ajouter au panier</a>
    </div>



  {% endfor %}

{% endblock %}

